$(function () {
/*
	$("#check_deleted_flg")
      .click(function() { 
    });
    
    //分類が変更された場合のイベント
    $("#head_input_m_class_check_id")
      .change(function() { 
	      	$.get(
			    '/m_audit_checks/',                 // 送信先
			    { input_day: $("#check_deleted_flg").val() , input_m_class_check_id: $(this).val(), remote: true},
			    function(data, status) {        // 通信成功時にデータを表示
			       $('#result').empty();
	               $('#result').append(data);
	             },
			    "html"                          // 応答データ形式 xml, html, script, json, jsonp, text
	            );
      	});
      	
*/    
})();

