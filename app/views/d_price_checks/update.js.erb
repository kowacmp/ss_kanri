// 編集エリアを再表示
$('#input_area').html("<%= escape_javascript(render :partial => 'form', :locals => { :d_price_check => @d_price_check,
                                                                        :input_flg     => true,
                                                                        :input_name    => 'input_area'}) %>");

// 登録済み一覧を再表示
$('#keys_area').html("<%= escape_javascript(render :partial => 'keys') %>");
