<%= form_for(@d_audit_changemachine) do |f| %>
  <% if @d_audit_changemachine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@d_audit_changemachine.errors.count, "error") %> prohibited this d_audit_changemachine from being saved:</h2>

      <ul>
      <% @d_audit_changemachine.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
