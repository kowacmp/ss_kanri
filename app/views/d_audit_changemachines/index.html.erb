<%=render :partial => 'header' , :locals => {:m_shop_id => current_user.m_shop_id,
	                                         :created_user_id => current_user.id,
	                                         :audit_date => Time.now.strftime('%Y/%m/%d') } %>
