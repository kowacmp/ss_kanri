<%= form_for(@access_log) do |f| %>
  <% if @access_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@access_log.errors.count, "error") %> prohibited this access_log from being saved:</h2>

      <ul>
      <% @access_log.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :access_user_id %><br />
    <%= f.number_field :access_user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
