<%= javascript_include_tag "modal" %><!-- モーダルを出すには再読み込みする必要あり -->
<table id='rounded-corner'>
	<tr>
	  <th colspan="4">車検予約カード獲得</th>
	</tr>
	<tr>
	  <th>月</th>
	  <th>日計</th>
	  <th>累計</th>
	  <td rowspan="6">
	  	<%= link_to '入力', {:controller => 'd_results', :action => 'reserve_index', :result_date => @result_date}, :id => "add_icon", :class => "modal" %>
	  </td>	  	  	  
	</tr>
  <% unless @syukei_reserves.blank?%>	
  <% @syukei_reserves.each do |syukei_reserve| %>
    <tr>
      <th class="input_no"><%= syukei_reserve[:month] %></th> 	
      <td class="input_no"><%= syukei_reserve[:nikkei] %></td> 
      <td class="input_no"><%= syukei_reserve[:ruikei] %></td>           
    </tr>
  <% end %>	
  <% end %>
</table>
    