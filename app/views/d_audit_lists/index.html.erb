<%=render :partial => 'header' , :locals => {:audit_date_from => Time.now.strftime("%Y/%m/%d"),
                                             :audit_date_to   => Time.now.strftime("%Y/%m/%d"),
                                             :kansa           => nil,
	                                         :shop_kbn        => nil,
                                             :audit_class     => "0" } %>
