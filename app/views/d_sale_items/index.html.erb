<h1>Listing d_sale_items</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @d_sale_items.each do |d_sale_item| %>
  <tr>
    <td><%= link_to 'Show', d_sale_item %></td>
    <td><%= link_to 'Edit', edit_d_sale_item_path(d_sale_item) %></td>
    <td><%= link_to 'Destroy', d_sale_item, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New D sale item', new_d_sale_item_path %>
